name: rtk
version: 1.0.0
description: "RTK Rewrite Plugin for Hermes — transparently rewrites terminal tool commands to RTK equivalents, achieving 60-90% LLM token savings. Requires rtk binary in PATH."
author: "ogallotti (original), Dannou (JarvisV3 integration)"
hooks:
  - pre_tool_call
provides_tools: []
